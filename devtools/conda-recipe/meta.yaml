package:
  name: crank
  version: "0.0.0"

source:
  path: ../..

build:
  number: 0

requirements:
  build:
    - python
    - numpy
    - setuptools
  run:
    - python

test:
  requires:
    - pytest
  imports:
    - crank

about:
  home: https://github.com/lpwgroup/crank
  license: MIT License
